# Copier en .env (jamais commité)
# Vide en dev : l'API utilise PGlite (PostgreSQL embarqué). En prod : URL PostgreSQL managé (Neon, Supabase…)
DATABASE_URL=
AUTH_SECRET=change-me
API_PORT=4000
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:4000
